# Match the library (upstream CMAKE_CXX_STANDARD 17).
#
cxx.std = 17

using cxx

hxx{*}: extension = h
cxx{*}: extension = cpp

# Every exe{} in this subproject is by default a test.
#
exe{*}: test = true

# The test target for cross-testing (running tests under Wine, etc).
#
test.target = $cxx.target
